/**
 * CosmicOverscrollGlow Component (Disabled)
 * Yellow overscroll animations and DOM overlays removed.
 * Overscroll transition is handled purely within the 3D space canvas.
 */
export default function CosmicOverscrollGlow() {
  return null;
}
